<p class="eyebrow"><span class="pixel" aria-hidden="true"></span><slot /></p>

<style>
  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    margin: 0 0 0.75rem;
    color: var(--ink-soft);
    font: 500 0.6875rem var(--font-mono);
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  /* Two pixels, ink and dither: the brand's vocabulary at its smallest. */
  .pixel {
    width: 12px;
    height: 6px;
    background: linear-gradient(90deg, var(--ink) 50%, transparent 50%), var(--dither);
    background-size: 12px 6px, 2px 2px;
    background-repeat: no-repeat, repeat;
  }
</style>
